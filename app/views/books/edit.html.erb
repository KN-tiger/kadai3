<h1>Editing Book</h1>

<%= form_with model: @book, url: '/books' do |f| %>

  <h4>title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %><br>

  <%= f.submit 'Update Book' %>


  <%= link_to "Show", books_path(book.id) %>| Back
